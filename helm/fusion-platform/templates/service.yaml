apiVersion: v1
kind: Service
metadata:
  name: {{ include "fusion-platform.fullname" . }}
  labels:
    {{- include "fusion-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fusion-platform.selectorLabels" . | nindent 4 }}
