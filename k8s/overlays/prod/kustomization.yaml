apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fusion-prod

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: items-service
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3
